fastapi==0.115.5
uvicorn[standard]==0.32.1

# Anthropic Claude API
anthropic>=0.40.0

# MCP client — [client] extra includes ClientSession and
# streamablehttp_client transport needed to speak MCP protocol
# Must match the server version (mcp[server]>=1.25,<2)
mcp[client]>=1.25,<2

# HTTP client used by the MCP streamable HTTP transport
httpx>=0.27.0

# SSE (Server-Sent Events) support for Streamable HTTP transport
# Required by mcp.client.streamable_http under the hood
httpx-sse>=0.4.0

# Loads .env file for local development
python-dotenv==1.0.1